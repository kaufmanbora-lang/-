# Orbit Chat

Мобильный веб-мессенджер с регистрацией, входом, общим чатом, личными сообщениями и живыми обновлениями.

## Безопасность

Версия `1.5.1` усиливает мобильную раскладку всех меню: нижние панели, липкие заголовки, безопасные отступы для телефона, компактный action-rail и новые дизайн-бейджи в списке чатов.

Версия `1.5.0` добавляет три крупных меню: опросы комнаты, доску задач и события с RSVP, плюс новый дизайн action-rail, карточек, бейджей и мобильной раскладки.

Версия `1.4.9` добавляет пересылку сообщений, заглушение чатов, экспорт переписки, личные заметки по чатам и быстрые slash-команды.

Версия `1.4.8` усиливает админскую блокировку: забаненный аккаунт больше не может войти, а старые сессии отзываются.

Подробности лежат в `SECURITY.md`.

## Что есть

- регистрация по email, нику и паролю;
- подтверждение почты при регистрации;
- восстановление пароля по почте;
- смена почты в профиле с подтверждением новой почты;
- вход в аккаунт;
- вход администратора по отдельному паролю;
- служебный аккаунт администратора;
- админская блокировка пользователей на весь сайт;
- админское удаление аккаунтов;
- просмотр жалоб в админ-панели;
- общий чат для всех игроков/друзей;
- личные сообщения между пользователями;
- групповые чаты;
- роли групп: владелец, админы, участники;
- управление группой: переименовать, добавить, удалить, повысить, снять админа, выйти;
- видеозвонки 1-на-1 в личных чатах;
- групповые видеозвонки из группового чата;
- аудиозвонки без камеры;
- устойчивее входящие звонки после push/перезагрузки приложения;
- диагностика звонка и кнопка приглашения в окне звонка;
- отправка фото и файлов;
- отправка файлов до 25 МБ;
- голосовые сообщения;
- исправленные голосовые после security-обновления;
- устойчивые голосовые сообщения в разных браузерах: webm/opus, ogg, mp4, aac;
- реакции на сообщения;
- избранные сообщения;
- фильтр только избранных сообщений в текущем чате;
- любимые чаты со звездой, которые сохраняются в аккаунте;
- черновики сообщений отдельно для каждого чата;
- копирование текста сообщения;
- копирование ссылки на конкретное сообщение;
- ответы на сообщения;
- редактирование и удаление своих сообщений;
- закрепленные сообщения в чате;
- поиск внутри выбранного чата;
- отметки прочтения и статус "печатает";
- бейджи непрочитанных сообщений в списке чатов и в заголовке приложения;
- аватарки аккаунтов до 8 МБ;
- сохранение людей в контакты;
- просмотр профилей людей;
- блокировка пользователей;
- жалобы на пользователя или сообщение;
- админ-панель жалоб в разделе безопасности;
- медиа-галерея чата;
- уведомления через браузер и push-подписки;
- PWA-режим: manifest, иконка приложения, service worker и установка на телефон;
- панель диагностики уведомлений с тестовым push;
- кнопка копирования ссылки на приложение в панели уведомлений;
- push-уведомления для новых сообщений и входящих звонков;
- поиск людей только через строку поиска по нику или точной почте;
- мобильный интерфейс для телефона;
- серверная база в JSON-файле;
- опциональная PostgreSQL-база через `DATABASE_URL`;
- готовый `render.yaml` для публикации на Render.

## Вход администратора

На экране входа есть отдельное поле `Пароль администратора`.

Если ввести правильный админ-пароль, приложение зайдет в служебный аккаунт `Администратор`. В профиле любого найденного пользователя появится кнопка `Админ: забанить`.

На Render пароль можно поменять через переменную:

```text
ADMIN_LOGIN_PASSWORD=твой-новый-пароль
```

## Почта для подтверждений

Чтобы письма подтверждения и восстановления пароля реально приходили, на Render нужно добавить SMTP или Resend.

SMTP:

```text
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-user
SMTP_PASS=your-password
SMTP_FROM=Orbit Chat <no-reply@your-domain.com>
PUBLIC_APP_URL=https://твоя-ссылка.onrender.com
```

Или Resend:

```text
RESEND_API_KEY=your-resend-key
MAIL_FROM=Orbit Chat <no-reply@your-domain.com>
PUBLIC_APP_URL=https://твоя-ссылка.onrender.com
```

Без этих переменных приложение не сможет физически отправить письмо на почту.

## База данных для настоящего релиза

Самый простой режим работает через JSON-файл. Для настоящего сайта лучше добавить PostgreSQL и переменную:

```text
DATABASE_URL=postgresql://...
```

Если `DATABASE_URL` есть, Orbit Chat хранит аккаунты, сообщения, группы, жалобы, настройки и новые загрузки файлов в PostgreSQL. JSON остаётся локальной резервной копией.

На Render можно создать PostgreSQL отдельно, скопировать Internal Database URL и вставить его в `Environment` сервиса Orbit Chat.

## Локальный запуск

```bash
npm install
npm start
```

Проверка перед загрузкой:

```bash
npm run check
```

Открыть:

```text
http://127.0.0.1:8790
```

На этом компьютере можно просто два раза нажать `START_APP.bat`: он запустит сервер и откроет правильную ссылку.

## Важно

Чтобы чат работал из любой страны и компьютер не должен был быть включен, проект нужно загрузить на GitHub и развернуть на Render. Для надежного хранения аккаунтов, сообщений и файлов подключи PostgreSQL через `DATABASE_URL`.

Для видеозвонков на телефоне открывай опубликованную HTTPS-ссылку Render. На обычной локальной `http://192.168...` ссылке телефон может не дать камеру, потому что браузеры требуют HTTPS. В группе нажми кнопку камеры сверху: приглашение уйдет участникам группы.

Push-уведомления используют пакет `web-push`. Render установит его автоматически через `npm install`. После входа в аккаунт нажми колокольчик в приложении, нажми `Включить`, разреши уведомления на телефоне и нажми `Тест`.

Для экрана блокировки на iPhone сайт должен быть открыт по HTTPS, добавлен на главный экран через Safari и запущен через иконку приложения.

Подробная инструкция для телефона лежит в `PHONE_PUSH_GUIDE.md`.
